diff --git a/dist/index.js b/dist/index.js
index a4af7341af4981b2896bb6840b4a465de73c45fb..9c9e681118bd39f372ba0336dc18d4adacfb3916 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -877,7 +877,12 @@ var InternalMastraMCPClient = class extends MastraBase {
   async convertInputSchema(inputSchema) {
     try {
       await $RefParser.dereference(inputSchema);
-      return "jsonSchema" in inputSchema ? inputSchema.jsonSchema : inputSchema;
+      const schema = "jsonSchema" in inputSchema ? inputSchema.jsonSchema : inputSchema;
+      if (schema && typeof schema === "object" && typeof schema.$schema === "string" && schema.$schema.includes("2020-12")) {
+        const { $schema, ...rest } = schema;
+        return rest;
+      }
+      return schema;
     } catch (error) {
       let errorDetails;
       if (error instanceof Error) {
